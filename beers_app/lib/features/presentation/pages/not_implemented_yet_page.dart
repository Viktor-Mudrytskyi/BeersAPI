import 'package:flutter/material.dart';

class NotYetImplementedPage extends StatelessWidget {
  const NotYetImplementedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Nothing here yet...',
        style: Theme.of(context).textTheme.headline1,
      ),
    );
  }
}
